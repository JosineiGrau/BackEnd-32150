<html lang="en">
	<head>
		<%- include("partials/head.ejs", {tituloWeb: "Mock products"}) %>
	</head>
	<body>
		<header><%- include("partials/header.ejs") %></header>
		<main>
			<section class="main-table">
				<%if (newsProducts.length === 0) { %>
					<h1>No hay productos</h1>
				<% } %>
				<%if (newsProducts.length > 0) { %>
					<table>
						<thead>
							<tr>
								<th>Id</th>
								<th>Name</th>
								<th>Price</th>
								<th>Image</th>
							</tr>
						</thead>
						<tbody>
							<% newsProducts.forEach(data => { %>
							<tr>
								<th><%= data.id%></th>
								<td><%= data.name%></td>
								<td>$<%= data.price%></td>
								<td>
									<img src=<%= data.image%> alt="hola"/>
								</td>
							</tr>
							<% }) %>
						</tbody>
					</table>
				<% } %>	
			</section>
		</main>
	</body>
	<footer><%- include("partials/footer.ejs") %></footer>
</html>