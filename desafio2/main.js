const Container = require("./container")

const product = new Container
    product.read()
setTimeout(()=>{
    // product.save({name:"cart",price:15})
    // console.log(product.getById(1));
    // console.log(product.getAll());
    // product.deleteById(1)
    // product.deleteAll()
},1000)